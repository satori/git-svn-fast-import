language: c
compiler:
    - clang
    - gcc
notifications:
    email: false
install:
    - sudo add-apt-repository ppa:svn/ppa -y
    - sudo apt-get update -qq
    - sudo apt-get install -y libapr1-dev
    - sudo apt-get install -y libsvn-dev
script:
    - make && make test
